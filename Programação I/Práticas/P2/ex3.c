#include <stdio.h>
#include <stdlib.h>

int main(){
    float a, b, c;    //valores
    printf("Valor: ");
    scanf("%f" "%f" "%f", &a, &b, &c);

    int d;  //divisor 
        d = 3;

    float e;    //média
        e = (a + b + c) / d;

    printf("A média dos valores %.2f, %.2f e %.2f é igual a %.2f", a, b, c, e);
}