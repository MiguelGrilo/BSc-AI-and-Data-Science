#include <stdio.h>
#include <stdlib.h>

int main(){
    int (a);
        printf("Digite o valor de a:");
        scanf("%d", &a);
    int (b);
        printf("Digite o valor de b:");
        scanf("%d", &b);

    a=a+1;
    while(a<b){
        printf("%d\n", a);
            a=a+1;
    }
}